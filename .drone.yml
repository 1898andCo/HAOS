---
kind: pipeline
name: amd64

platform:
  os: linux
  arch: amd64

steps:
- name: test
  image: rancher/dapper:v0.4.2
  privileged: true
  commands:
  - dapper test
  volumes:
  - name: docker
    host:
      path: /var/run/docker.sock